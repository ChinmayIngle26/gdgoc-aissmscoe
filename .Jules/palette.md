## 2024-07-07 - Dynamic Aria-Label on Toggle Buttons
**Learning:** Using dynamic `aria-label` along with `aria-expanded` and explicit `focus-visible` styles significantly improves the accessibility of interactive icon-only menu toggles.
**Action:** Always pair `aria-expanded` and dynamic `aria-label` with `focus-visible` classes when using icon-only buttons for toggling menus.
